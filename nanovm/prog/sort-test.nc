func main ()
    qint i = 0Q; qint max = 256Q; int value;
    qint address;
    
    int null = 0; int one = 1; int f;
    value = max - 1;
    int sort_thread;
    
    int _a[max];
    
    for;
        var2array (value, _a, i);
        
        value = --;
        i = ++;
        f = i < max;
    next f;
    
    getaddress (_a, address);
    @sort (address, max, null);
    get (sort_thread);
    
    printn one;
    print "sorted:";
    printn one;
    
    i = 0;
    for;
        array2var (_a, i, value);
        
        print value;
        printn one;
    
        i = ++;
        f = i < max;
    next f;
    
    exit null;
funcend

#include <sort.nch>
